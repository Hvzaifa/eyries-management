# Progress Tracker

Update this file every time a step is reviewed and confirmed working. This is the first thing to read each session — it tells you (and Claude Code) exactly where to resume.

**Current phase:** Phase 8 — Bot sales (not started)
**Current step:** Phase 8 Step 1 — see `docs/phases/phase-8-bot-sales.md`. **Confirm scope with the owner before starting.**
**Status:** Phases 1–7 complete. Database cleared 2026-09-20 for clean re-entry (lookups and logins kept); real bookings are being entered through the UI. Entry rules tightened 2026-09-20 (the imported data is history, not the standard — see `decisions.md`). Phase 7 rests on owner rules recorded in `decisions.md` (rulings 9–17) plus the IATA payment rules of 2026-09-22. Two items are flagged there for an owner decision and neither blocks Phase 8: whether a pre-payment refund should count toward `total_refunded`, and whether a branch should be able to email its own agents.

---

## How to update this file

After a step is built and you've personally checked it against real sample data, change its status to `Done` and move "Current step" to the next one. If a step is in progress, mark it `In progress` so a new session picks up correctly instead of restarting it.

---

## Log

| Date | Phase | Step | Status | Notes |
|---|---|---|---|---|
| 2026-08-22 | Phase 1 | Step 1: DB schema | Done | Project setup initialized, Supabase migration verified, lookup tables seeded (3 licenses, 4 branches, 4 airlines). Schema fully applied + made idempotent during hardening pass; dashboard_totals view completed with paid/refunded totals |
| 2026-08-23 | Phase 1 | Step 2: Authentication | Done | Auth implemented, login works with sample users. Public signup removed (admin-created accounts only), seed-users.ts rewritten to Supabase Admin API |
| 2026-08-23 | Phase 1 | Step 3: PNR List Dashboard | Done | TanStack Table with sorting/filtering, urgency colours (pending-only rounds), dashboard totals from SQL view, PKR formatting, light cream theme, vitest + 12 urgency unit tests |
| 2026-08-23 | Phase 1 | Step 4: PNR detail page | Done | /pnrs/[id] with all core fields, ordered EMD rounds, ticketing + parent/child sections, activity history; shared AppHeader; rows linked from dashboard |
| 2026-08-23 | Phase 1 | Step 5: Manual entry / edit form | Done | Create + edit forms with role guards, activity logging per field, duplicate-PNR two-step warning, unit-tested EMD-1 suggestion (60-day gap resolved to 15%, >90 → 15%, <7 hides round section) |
| 2026-08-24 | Phase 1 | Step 6: Excel import | Done | Real sheet imported: 972 PNRs / 1727 rounds / 956 ticketing rows; SV-only EMD policy engine live; deadline_date now nullable; 119 rows flagged in flagged-rows-review.csv awaiting owner review |
| 2026-08-24 | Phase 1 | Post-import data pass | Done | 465 past-trip PNRs → completed (activity-logged); airlines renamed (9P/FZ/PF/UL); dashboard defaults to outbound-date ascending |
| 2026-08-24 | Phase 1 | Step 7: Deadline-check job | Done | Cron route + Resend email built, verified against test deadline. |
| 2026-08-25 | Phase 1 | Data governance pass | Done | Sheet data declared immutable; >100% replacement pass undone (55 PNRs restored to original sheet rounds, verified 1727 total); status definitions finalized: completed = outbound passed + full EMD refunded (564), cancelled = 4 strict conditions (0 qualify); EMD-2 deadlines from dates-band (88 set); open items in data-review-flags.md |
| 2026-08-26 | Phase 1 | Date-fix re-import | Done | Workbook stores dates as midnight-PKT (float artifact 12s short) — parser fixed, full re-import from updated sheet; cell-by-cell verification: 0 mismatches across 1005 records; 580 completed / 425 active; flagged list now 121 (incl. 2 blank-investor rows) |
| 2026-08-27 | Phase 2 | Step 1: Parsing endpoint | Done | Integrated local Ollama API (Gemma3:4b); AI structured parsing returns fields + confidence flags. |
| 2026-08-28 | Phase 2 | Step 2: Review screen | Done | Paste-and-parse form working; low confidence fields visually flagged; saves through same core path as manual entry. |
| 2026-08-29 | Phase 2 | Step 3: Real-world testing | Done | Validated against test airline formats, adjusted prompts based on failures; completed and documented. |
| 2026-08-30 | Phase 3 | Step 1: Auto-create Round 2 | Done | EMD Round 2 is auto-created for new PNRs; Add Round modal implemented for manual addition of 3rd/4th rounds. |
| 2026-08-31 | Phase 3 | Step 2: Refund recording | Done | Added Refund button to EMD rounds allowing status = refunded to be set; logs to activity_log |
| 2026-08-31 | Phase 3 | Step 3: Refund log view | Done | Added /refunds page querying refunded_emd_rounds view directly, linked in AppHeader |
| 2026-08-31 | Phase 3 | Step 4: Airline emails | Done | Added Email Airline modal; integrated Resend using eyriesholiday@gmail.com; logs email type to activity_log |
| 2026-09-01 | Phase 4 | Step 1: Split screen | Done | splitPnr server action with transaction; child PNR created with airline-assigned code, new investor company, optional different inbound date; parent seats reduced; EMD rounds recalculated proportionally (Scenario 1: EMD-1 paid → only EMD-2 split; Scenario 2: no EMD paid → all rounds split). SplitPnrButton modal with validation. Decisions logged in decisions.md. |
| 2026-09-01 | Phase 4 | Step 2: Parent/child navigation | Done | Added bidirectional links on the PNR detail page; parent shows all child links with seat breakdown and investor names; child shows link to parent. |
| 2026-09-07 | Phase 3/4 | Refinements | Done | Refined UI: "issued" replaced "pending" status, splitting allows editable outbound date, EMD issuance date fixed to current date, EMD Number validated (13 digits, spaced format), required field. |
| 2026-09-07 | Phase 3 | Bulk Refunds | Done | Implemented Head Office bulk refund page allowing batch fetching by PNR codes and batch refund processing. |
| 2026-09-07 | Phase 3 | License Tracking | Done | Added `licenseId` relation to `EmdRound`, tracking which specific license paid for each EMD round. |
| 2026-09-07 | Phase 3 | Batch Emails | Done | Replaced individual PNR email feature with a Batch Emails dashboard page, generating grouped emails per airline. |
| 2026-09-07 | Hardening | Critical-bug pass | Done | Full-codebase read. Four criticals fixed: (1) both SQL views bypassed RLS — 1,338-row refund ledger publicly readable, now `security_invoker = on` + grants revoked, **awaiting `db/apply-security.sql` on live**; (2) `db/schema.sql` never enabled RLS, so any rebuilt DB came up world-readable/writable — now explicit for all 8 tables; (3) `next build` failed on 13 ESLint errors, nothing could deploy — fixed with real types, build now exits 0; (4) daily deadline cron was redirected to /login by middleware and had never run in production — `api/cron` now exempt. Non-critical findings (refunds page null crash, unvalidated bulk refunds, batch-email open relay, seeded passwords, xlsx CVEs) documented but NOT fixed. |
| 2026-09-07 | Hardening | High-severity pass | Done | (5) /refunds crashed on `8N32CG` round 2 (refunded, no date/amount) — columns typed nullable, both cells guarded, all 1,338 rows verified rendering; (6) batch airline email was an open relay — recipient now must be one of the airline's recorded contacts, PNRs must belong to that airline, hardcoded personal Gmail removed, and subject/body/recipient now logged per phase-3 Step 4; (7) hardcoded seed passwords removed, env vars mandatory — **owner must rotate if the seed was ever run without them**; (8) refund writes validated via new tested `src/lib/refunds.ts` (18 tests) — rejects negative/NaN/impossible dates/double refunds/blank amounts/duplicate rows, bulk is now all-or-nothing. Refund-exceeds-EMD deliberately NOT enforced (owner's call; 1 live row already exceeds). Build 0, lint 0, 99/99 tests. |
| 2026-09-07 | Hardening | Medium + low pass | Done | **Critical view fix APPLIED to live DB** — anon now gets 401 on both views (was 1,338 rows); app unaffected. Medium: `updatePnr` made transactional; EMD-1 deadline now band-based per owner ruling (short-notice bookings no longer created overdue) + gated to policy airlines, 16 tests; `recordEmdRefund` reads pnrId from the round not the form; PNR-code list branch-scoped (HQ 1014 / RWP 392 / unresolved 0); xlsx 0.18.5 → 0.20.3 via SheetJS CDN (both CVEs clear); Excel upload capped ~5MB; API no longer leaks upstream errors; 45s per-provider LLM timeout. Low: alert-email escaping completed, `/auth/callback` open redirect closed (new `safe-redirect.ts` + 8 tests), docstring/README drift fixed, dead code and stray files removed. Lint 29 problems → 3 warnings. Build 0, 115/115 tests. Outstanding: next@16 advisories, dashboard pagination. |
| 2026-09-07 | Hardening | EMD deadline scope | Done | Owner clarified the band offsets are **EMD-1 only**; EMD-2 follows the SV policy (departure−20/10/7/5), and a single-EMD booking keeps EMD-1 timing. Verified in code — no scope change needed. Checking it exposed a real defect: the two anchors cross on short-notice bookings, so EMD-2 fell due the same day as EMD-1 at 8 days out and a day BEFORE it at 7 days out, which also broke the PNR TL rule. New `clampEmd2Deadline()` applied to all three EMD-2 paths (create, form preview, nightly backfill); 9 tests incl. a sweep proving every band now orders correctly. Build 0, 124/124 tests. |
| 2026-09-07 | Hardening | Cleanup / YAGNI pass | Done | Ollama removed (local-only provider that always failed on Vercel) and the Groq→Cerebras→OpenRouter chain flattened into one `buildCandidates()`. Dead code deleted after verifying zero importers: the admin/staff/viewer compat shims + `scripts/test-auth-roles.ts` that tested them, `canCreatePnr`, `canEmailAirline`; `escapeHtml`/`dashboardUrl` made module-private. **`prisma/seed-sample-pnrs.ts` removed — `npm run db:seed` was inserting 8 fake `TEST-*` bookings, so the documented setup command would have polluted production.** Structure: `lib/types/auth.ts`→`lib/auth.ts`, `app/pnr-table.tsx`→`components/`, and the 1,063-line `actions.ts` split into `actions/{pnr,emd,email}.ts` with shared helpers moved to `lib/form.ts` + `lib/server/{guards,pnr-tl}.ts`. Kept the legacy-import tooling on purpose (documented deliverable, tested, needed for a rebuild). ESLint 0 problems, 124/124 tests, build green. |
| 2026-09-07 | Data | Full re-import + completion pass | Done | Wiped and re-imported from `Groups EMD Master Sheet.xlsx`. Fixed two blockers first: the importer still wrote the renamed `pending` status (DB would have rejected every unrefunded round) and has no delete step (a re-run would have duplicated everything). Backed up all 7,303 rows to `backups/` before wiping; kept lookup tables so branch logins keep working; reset the sr_no sequence. Imported **1,501 PNRs / 2,589 rounds / 1,483 ticketing**, SR#1–1501, 34 children linked. New `scripts/apply-completion-rule.ts` marked **644 completed** (outbound passed + at least one round + all rounds refunded), each activity-logged; verified 0 false positives and 0 missed. Final: 857 active / 644 completed. **120 sheet rows flagged and NOT imported** (61 missing payment %, 59 no complete round, plus duplicate pairs) — awaiting owner review. |
| 2026-09-07 | Docs | Documentation restructure | Done | Added `docs/README.md` as the index + required reading order, and `CLAUDE.md` now opens with "Before you write any code" pointing at it. **Rebuilt `decisions.md`: 14 entries had been appended below the template section, so the log silently split in two and a reader would have missed a third of it** — now 70 entries in one chronology with a month-grouped index. New `docs/operations.md` runbook (which commands write to prod, schema-change rules, backups, security posture, re-import procedure, cron). Raw AI transcripts (~12k lines) deleted on owner instruction after verifying all 15 owner turns they contained were already recorded in `decisions.md` (still in git at 5798601); `parsing-notes.md` → `docs/reference/`; index READMEs for phases/reference/sessions. All 31 internal links verified. |
| 2026-09-19 | Phase 5 | Step 1: Ticketing fields on PNR detail | Done | Ticketing section now always shows on the detail page (empty state when no row) with a Head-Office-only edit modal; new tested `src/lib/ticketing.ts` (SV 72h deadline = outbound−3d, impossible-date rejection, and the owner's ticket-count rule: issued ≤ seats, balance = seats − issued, calculated not typed); `saveTicketing` upserts transactionally with a per-field activity log; SV bookings are now created with the ticket-issuance deadline pre-filled; the **existing** daily job also reports name-update and ticket-issuance deadlines, in their own table in the same email. Unissued seats after a passed issuance deadline show as "considered cancelled" — derived, never stored. Verified against live data: 48 ticketing deadlines fall inside the 2-day window; the write path was exercised in a rolled-back transaction; 0 bookings flagged as cancelled today (the rejected "blank means zero" reading would have wrongly flagged 125). Owner-reported bug fixed the same day: ticketing edits were written to `activity_log` but the detail page's history query never asked for them. 168/168 tests, lint 0, build green |

| 2026-09-19 | Docs | Selling side documented | Done | No code. `architecture.md` amended — the selling side is in scope (penalty EMD and ticket loss stay out), with the bot and `/api/v1` in the diagram; `business-rules.md` gained the seat-ledger rule, the agent total and its payment schedule, and the bot's price/hold/sale rules; `data-model.md` gained six tables (`agents`, `agent_assignments`, `agent_recoveries`, `bot_allotments`, `bot_bookings`, `api_clients`) and a note that API writes log with a null user; `decisions.md` records all 24 owner rulings; new `phase-6-seat-ledger-agents.md`, `phase-7-agent-money.md`, `phase-8-bot-sales.md`. **Nothing outside `docs/` changed** — no schema, no Prisma, no code |

| 2026-09-19 | Phase 6 | Step 1: Agents | Done | `agents` table created on production via new `db/apply-agents.ts` (dry-run default, one transaction, verifies after) and mirrored in `db/schema.sql` + Prisma. New tested `src/lib/agents.ts` (18 tests): `name_key` normalisation so one agent cannot exist twice under two spellings, email validation because a dues notice may only go to a recorded address, and `agentVisibilityFilter`/`canEditAgent` that **deny** an unresolved branch rather than falling open. `/agents` list + create/edit modal, linked in the header for both account types. Verified on live data: HQ sees all 3 test agents, RAWALPINDI 1, LAHORE 1, unresolved branch 0; all three duplicate spellings rejected by the database (P2002); anon key gets `200 []` on the table and 401 on writes, same posture as `pnrs`. Test rows rolled back — table left empty. 186/186 tests, lint 0, build green |

| 2026-09-19 | Phase 6 | Step 2: Seat ledger + assignments | Done | `agent_assignments` applied to production via `db/apply-agent-assignments.ts` (6 check constraints incl. charge-XOR-discount, partial index on live rows) and mirrored in `schema.sql` + Prisma. New tested `src/lib/inventory.ts` (22 tests): `seatLedger` derives agent / bot / unassigned from `pnrs.seats` **without ever writing it**, plus assign/release/move/seat-change validators and `holderKind`. Actions in `actions/assignments.ts` lock the PNR row and recompute the ledger **inside** the transaction; release and move are head-office-only. Splits and seat edits now respect the ledger; `validateSplit`'s message reworded ("available to split", not "holds"). Verified on live data: two concurrent attempts to take all 50 seats → exactly one succeeded, no over-allocation; split and seat-reduction refused while assigned; releasing returned the seats; `pnrs.seats` unchanged; test rows cleaned up. 208/208 tests, lint 0, build green |

| 2026-09-19 | Phase 6 | Step 3: Seat ownership UI | Done | Seat-ownership panel on the PNR page (proportional bar, per-agent rows, assign / release / move dialogs, the last two Head-Office-only); derived **Holder** column + filter on the dashboard (Unassigned / agent name / Mixed, with the agent-vs-free split shown on Mixed); read-only `/agents/[id]` listing the bookings an agent holds, with visibility applied **in the query** so a branch cannot read another branch's agent by URL. `agent_assignments` added to the detail page's history query **with** the feature, and keyed on all assignments incl. released ones so "seats returned" survives the release. Perf: the nested include cost ~200ms on the 1,501-row dashboard query, so assignments are read as one flat query in parallel instead. Verified on live data: 10+5 seats → `Mixed`, 35 free; all seats to one agent → that agent's name; release → `Unassigned`; history visible throughout; branch and HQ agree; `pnrs.seats` unchanged; rows cleaned up. 212/212 tests, lint 0, build green |

| 2026-09-19 | Phase 6 | Step 4: Legacy agent mapping | Done | `scripts/map-legacy-agents.ts` (dry-run default, gitignored review CSV) + tested `src/lib/legacy-agents.ts` (22 tests against the real investor strings). First pass proposed only unambiguous names and flagged 49; **owner ruled instead that every value except Company Investment becomes an agent named exactly as recorded, with no seat splitting** — so `KJ/QFC/MAQBOOL` is one agent, codes stay in the name, and each booking's full seat count transfers. **Committed to production: 126 assignments across 42 new agents**, each activity-logged. Verified after: 0 bookings over-allocated, the 4 part-seat assignments are the owner's own UI tests, `pnrs.seats` and `total_emd_value` untouched, dashboard now reads 43 named holders with 5,670 active seats agent-held and 46,259 unassigned. Phase 8's prerequisite is now met. 234/234 tests, lint 0, build green |

| 2026-09-20 | Rules | Entry rules tightened for clean data | Done | Owner ruled the imported data is history, not the standard. New tested `src/lib/booking-entry.ts` (10 tests): unique PNR code (case/space-insensitive), fixed segment list (Umrah/Employment/Tour), and `COMPANY INVESTMENT` stamped on every new booking. Refunds can no longer exceed their round's EMD amount (5 new tests, both refund paths). `investor_company` removed from the create form and the split form; imported names shown read-only and **never rewritten by an edit**. Holder column relabelled to the business's words: Company Investment / agent / B2C / Bot / Mixed. Guards added so tightening cannot break old rows: the duplicate check skips the booking being edited, and an unchanged out-of-list segment is left alone. Measured on live data: 0 codes rejected, 0 duplicates, 0 segments outside the list, 0 over-refunds — no existing row is affected. 249/249 tests, lint 0, build green |

| 2026-09-20 | Rules | Holder wording corrected | Done | Owner correction: "Mixed" wrongly covered one agent plus company seats — it means several agents. Holder now reads `Company Investment` / the agent's name when a booking is wholly theirs / `QFC Group (20) + Company (30)` when part / `QFC Group + KJ Travels` when shared, with counts shown only where the company still holds seats. "Mixed" removed entirely. Label and filter split apart: `holderFilterKeys` lets a filter on one agent find the shared bookings too, which a filter on the displayed sentence would miss. Panel wording follows ("with the company", not "unassigned"). Verified live: 1,374 Company Investment, 127 wholly with one agent, the 2 part-transferred bookings render as specified, no "Mixed" anywhere. 257/257 tests, lint 0, build green |

| 2026-09-20 | Data | Database cleared for clean re-entry | Done | Owner instruction: the imported data goes, real data will be entered through the UI. Backed up all 10 tables to `backups/2026-09-20T13-46-38/` (4.6 MB, gitignored) first, then deleted via new `scripts/reset-transactional-data.ts` (dry-run default, one transaction): 1,503 bookings, 2,592 EMD rounds, 1,484 ticketing rows, 34 allocations, 41 agents, 131 assignments, 5,143 log entries. Kept 8 licenses, 12 branches, 9 airlines (branch logins resolve by branch name; airlines carry contact emails and the SV policy code) and all auth accounts. SR# sequence restarted, so the first booking entered reads SR#1. Verified: dashboard shows 0 rows, lookups intact |

| 2026-09-20 | Phase 7 | Step 1: What an agent owes | Done | New tested `src/lib/agent-money.ts` (26 tests): `agentTotal` = seats × fare + charge − discount + (seats × airline tax when ticked), `agentMargin` = charge − discount with **tax never counted as profit** (ruling 15), summed in whole paisa. The owner's worked example is a required test — 10 seats × 100,000 + 5% + 20,000 tax = **PKR 1,250,000**, margin 50,000. Terms are set per agent per booking from the seat-ownership panel (one adjustment, charge *or* discount, so the XOR rule cannot be expressed wrongly), with a live total that uses the same function the server validates with; each agent's row now shows what they owe and the margin. `saveAssignmentTerms` locks the PNR and re-reads the assignment inside the transaction, logs the change in words ("5% of fare"), and is open to branches on their own bookings (ruling 6). Three open questions answered and recorded in `decisions.md`: a percentage is of the **base fare**, a discount may not exceed it (a charge has no ceiling — flagged for the owner), and a value whose term is switched off is dropped. Verified live in rolled-back transactions: the example totals exactly, two agents on one booking differ independently, both database checks fire, `pnrs.seats` and `total_emd_value` untouched. 283/283 tests, lint 0, build green |

| 2026-09-20 | Phase 7 | Step 2: Recoveries | Done | `agent_recoveries` applied to production via new `db/apply-agent-recoveries.ts` (dry-run default) and mirrored in `schema.sql` + Prisma, RLS on. `agent-money.ts` gained `agentBalance` (total / recovered / outstanding / **credit**), `sumRecoveries` and `validateRecovery` in the shape of `refunds.ts` — 17 new tests, 43 in the file. Payments are recorded from the agent's row in the seat-ownership panel, which lists what is already there so a double entry is caught before it is made; the panel now reads Paid / Outstanding / "Settled in full" / "in credit". Three defaults recorded in `decisions.md` for owner confirmation: a wrong payment is **deleted** (head-office only, logged) rather than cancelled by a negative row, an **overpayment shows as credit** (ruling 13's own rule), and a payment cannot be dated in the future against the **server's** PKT date. Recovery log entries are keyed by the assignment, not the recovery, so a deleted payment stays in the history — and `agent_recoveries` was added to the detail page's history query **with** the feature. Verified live in rolled-back transactions on the owner's example: 150,000 → 1,100,000 outstanding, paid in full → 0, +50,000 → 50,000 credit; the database refused 0 and −5,000; the payment reference `CHQ-SECRET-99881` does not appear in the log line (rule 8); anon key `200 []` / `401`. 300/300 tests, lint 0, build green |

| 2026-09-20 | Fix | Holder filter shows that holder's share | Done | Owner's report: filtering the dashboard to an agent kept the right bookings but described the whole booking — AFY883's 99 seats (Ansar e Madinah 30, Eyries Holidays 30, company 39) read as 99 seats for whichever agent was picked. New tested `src/lib/holder-view.ts` (22 tests) projects each row to the selected holder **before the table sees it**, so rows, sorting, the visible count and the cards all agree with no second code path. Seats are the holder's own; EMD value is `heldSeats × fare`, exact rather than prorated; paid and refunded are divided by seat share with the **odd paisa left with the company**, so a booking's holders always re-add to the booking. Holder cell reads `Ansar e Madinah (30 of 99)`; cards relabel to *Seats Held* / *EMD Value (Share)* under a line naming the holder with a clear-filter link. Four review catches closed before shipping: the projection is `useMemo`d (an inline `map` would have invalidated the memoised model the cards read), `globalFilterFn` now stringifies primitives only (`holderParts` would have made a search for "o" match every booking), shares cannot exceed a booking whose rows over-allocate, and a holder of every seat keeps a `null` EMD value as an em dash. `Company Investment` and `B2C / Bot` are now reserved agent names. Verified by running the real pipeline over the live bookings: every holder's figures correct and every booking reconciles exactly; 4,500,000 splits 1,363,636.36 / 1,363,636.36 / 1,772,727.28 to the paisa. 327/327 tests, lint 0, build green |

| 2026-09-20 | Phase 7 | Step 3: When it is due | Done | New tested `src/lib/agent-dues.ts` (30 tests): `emdShareDue` = `(agent seats ÷ PNR seats) × the EMD the airline holds` less what they have paid, due 3 days before the earliest open round's deadline; `finalBalanceDue` = everything left, due 3 days before the ticket-issuance deadline (for SV that is outbound − 6); `agentDues` returns both plus the single `next` obligation. **The owner's extension cycle is a required test and was also replayed through real database rows** in a rolled-back transaction: R1 450,000 → 150,000 due by 07 Oct; R1 refunded + R2 450,000 issued → **0 due**, the payment standing as credit; R3 2,550,000 → 850,000 by 29 Oct. Three things the ruling did not state are now recorded in `decisions.md`: "held" = `issued`/`paid`/`refund_requested` (`refunded` excluded, which is what stops an extension charging twice; `expired` excluded as a reading, flagged for confirmation), the EMD share and the balance are **never added** (milestone inside the total), and one payment counts against both. No ticketing deadline → no due date and no reminder, shown with the reason. The schedule now reads off `/agents/[id]` — seats, owes, paid, outstanding and a colour-coded next-due per booking, with an Owes/Paid/Outstanding summary — and the booking's seat-ownership panel carries the same next-due chip from the same functions. Checked against the owner's live bookings: MAQBOOL TRAVEL owes 2,280,000 by 23 Sep on ABC321, and AFY883's two agents each owe their own 3,450,000 / 3,480,000 share. 357/357 tests, lint 0, build green |

| 2026-09-21 | Rules | EMD deadlines are issuance deadlines | Done | Owner correction to a Phase-1 misunderstanding: `emd_rounds.deadline_date` is the date the next EMD — **or the tickets** — must be **issued** by, which is what secures the PNR. It was built and documented as a payment deadline throughout. Payment timing is an IATA matter, explicitly deferred. The SV policy figures now live in `emd1PolicyDaysToIssue` (14/10/3/3/1/0, auditable against the airline's sheet) with the owner's 3-day safety margin as one constant applied to them, **floored at immediate** per his instruction — so the bands store 11/7/immediate×4. This knowingly reverses part of the 2026-09-07 ruling; a deadline of today reads "due today", not "overdue", so the complaint behind that ruling does not return. The 2nd EMD keeps the policy's own dates (departure − 20/10/7/5), no margin. One behaviour change: the EMD-2 backfill skips a fully ticketed booking, since ticketing is the other way to meet the deadline — flagged as a reading, not a stated rule. Wording corrected wherever it asserted "payment": `agent-dues.ts`, `pnr-tl.ts`, the daily alert body, the EMD-rounds subtitle and the booking form. `payment_pct`, `status = 'paid'` and the dashboard totals deliberately unchanged. New `scripts/emd-deadline-review.ts` (report-only by default) run over the 8 live rounds: all four 2nd-EMD dates already match the policy, three 1st-EMD dates differ — left untouched pending the owner's call. 359/359 tests, lint 0, build green |

| 2026-09-21 | Fix | EMD rounds: issuing, deadlines, amounts, statuses | Done | Five owner-reported defects. **(1)** A branch could issue EMDs — not via Add Round, which was already HQ-only, but through the new-booking form's "First EMD round" section. That section is gone for everyone; `createPnr` no longer writes rounds at all. **(2)** A booking now starts with the **deadline to issue the 1st EMD** instead of a round, stored in the existing `pnr_tl_date`; the booking page shows "1st EMD to be issued by X" above the rounds, becoming "2nd EMD…" once the first is issued. **(3)** The deadline left the round card — it is the time limit for the *next* EMD, not that round's own — along with per-round overdue styling. **(4)** EMD amount is now `seats × fare × %`, pre-filled and editable, following the percentage until someone types over it, with the arithmetic and an "edited" marker shown. **(5)** Statuses reduced to **issued / refunded** via `db/apply-emd-statuses.ts`; `paid`, `refund_requested` and `expired` are gone because EMD payment is an IATA matter not modelled yet. Knock-on: the dashboard's "Total Paid" card had nothing left to count and is now **"EMD Issued"** — the deposit the airline holds (`totalPaid` renamed `totalIssued` rather than left meaning something else); `splitPnr` and the EMD-2 backfill now read "settled" as `refunded`. Deadlines come from the policy where one exists and are typed in where it does not — the airline hands out policies, not per-PNR time limits. Verified live in rolled-back transactions: a branch-shaped booking gets 0 rounds and a policy−3 deadline; round 1 pre-fills 15% / PKR 937,500 on 50 seats at 125,000 and secures to outbound−20; deposit + balance re-add to the booking's exact value across three bands; the database refuses all four removed statuses. 368/368 tests, lint 0, build green. **Next: bulk EMD issuance from the dashboard** (tick bookings → issue, EMD number typed per booking) |

| 2026-09-22 | Feature | Bulk EMD issuance (HQ) | Done | The screen the owner asked for when issuing was split from booking. Head Office ticks bookings on the dashboard — checkbox column, "select all" scoped to the **visible** rows, selection held by booking id so it survives sorting and filtering — and a bar offers **Issue EMDs**, carrying the ids to `/pnrs/bulk-emd?ids=…` (a URL, so the screen survives a reload and can be handed to a colleague). Each row arrives pre-filled from the booking and its policy: round number, %, amount and the new time limit, all editable; **the EMD number is the only thing typed**, being the only thing that cannot be derived. New `nextEmdSuggestion()` in `lib/emd.ts` is now the single source of those figures, replacing the copy the detail page had — the single and bulk paths can no longer propose different amounts. The batch is **all-or-nothing**, fully validated first, naming the failing booking; the same booking twice and the same EMD number on two bookings are both refused; ineligible bookings (cancelled, completed, no seats) are listed with the reason rather than dropped. Dedup found on the way: the 13-digit EMD-number pattern existed in three places and is now one tested `isValidEmdNumber()`. Verified live in a rolled-back transaction across three policy bands — 50×125,000 → 15% = 937,500; 30×115,000 → 30% = 1,035,000; 20×100,000 → 50% = 1,000,000 — each booking ending with one round and its next issuance deadline moved to what that EMD secured. 383/383 tests, lint 0, build green |
| 2026-09-22 | Rules | IATA payment deadlines | Done | The second clock on every EMD, from the owner's `IATA-Calender.pdf`. **An EMD issued inside a billing period must be paid on that period's remittance day** — 48 four-times-per-month periods covering 1 Jan–31 Dec 2026, transcribed into `src/lib/iata-calendar.ts` (the 365 `EasyPay` rows are excluded as instructed; the lag runs 7–10 days with no pattern, so nothing is extrapolated and an EMD outside the calendar shows "not in calendar", never a guessed date). The deadline is **derived, never stored**, so republishing the calendar moves every figure. New `emd_rounds.payment_date` records that money went — a **column, not a status**, because a paid EMD is still `issued`; nothing backfilled. **The big rule learned here: rounds 3 and 4 are rounds 1 and 2 re-issued** — a refund can cancel the billing and move the bill a cycle, so a refund is not always money coming back. **The cut-off is the BILLING window, not the payment day** (owner correction the same day): the refund must land on or before the period's billing-to date, and the replacement EMD be issued after it. An EMD issued 20 Sep must be refunded by 23 Sep and re-issued 24 Sep to move from 30 Sep to 7 Oct; refunding on the 26th moves nothing. A round refunded after its window but never paid is **still owed and is not `issued`**, so no query filters on `status` — `iataPaymentState()` decides and everything filters on its verdict. Each owed round also shows its roll-by date. Also corrected: refunds are processed by **IATA, not the airline**. New HQ-only `/iata` groups what is owed by remittance day with a per-day total; the dashboard gets its own IATA Payment column with its own colour beside the issuance one (two indicators, owner's choice); the daily email gains a third table on the same future-only 2-day window. Verified live in a rolled-back transaction across four periods, including the roll-forward end to end (round due 7 Oct → refunded → re-issued 8 Oct → due 22 Oct). 441 tests, lint 0, build green. **Flagged for a decision:** a pre-payment refund should not count toward `total_refunded`, which currently sums every refunded round |
| 2026-09-22 | Phase 7 | Step 4: Telling the agent | Done | **Phase 7 complete.** Agent dues join the existing daily job as a fourth table — never a second job — on the same future-only 2-day window. **The email never writes to an agent:** it reports who is due with a link, and sending is a human click on `/agents/[id]`. The recipient is a select of that agent's recorded addresses and is re-checked server-side in `validateAgentNotice()`; an agent with no address on file cannot be sent anything, which is deliberate — a fallback to a typed address is the open relay the airline email was (2026-09-07). Notice text names booking, seats, amount, date and purpose only, describing the EMD share as part of the total, never an extra charge (ruling 13). Undated obligations and inactive agents are never chased. Every notice is logged against the hand-over with recipient, subject and body, and appears in the booking history (the read query already listed `agent_assignments`). `/agents` gained seats/owes/paid/outstanding/margin/next-due — read in one query, not per agent — and `/agents/[id]` gained margin and the send button. **Refactor:** the list, the detail page and the alert each built `AgentTerms` and called `agentDues` separately; all three now share `duesForAssignment()`, the same fix as `nextEmdSuggestion()` on the EMD side. Verified live in a rolled-back transaction and reconciled by hand: 10 of 30 seats at 100,000 + 5% + 20,000 tax = PKR 1,250,000 owed, margin 50,000; EMD share 10/30 × 450,000 = 150,000, less a 100,000 recovery = 50,000 due 23 Sep (3 days before the round's 26 Sep deadline); the guard refused an unrelated address, a lookalike suffix and an agent with no email. 479 tests, lint 0, build green. **Flagged:** whether a branch should be able to email its own agents (`canEditAgent` is the guard) |
| 2026-09-22 | Rules | Agent EMD money: per round, no deadline | Done | Owner instruction that **withdraws** part of ruling 13. The agent's EMD share now has **no payment deadline** — staff collect before each round is issued — so the 3-day rule survives for the final balance only. The share is shown **broken down per round** on the booking's seat-ownership panel and the agent page: a breakdown of one running balance (payments stay one pot, no round reference added to `agent_recoveries`, which is what keeps the credit rule working when a round is refunded and replaced). **Agents see rounds 1 and 2 whatever the booking calls them** — refunded rounds are omitted and survivors renumbered, so the IATA roll-forward that turns rounds 1–2 into 3–4 does not have an agent asking why they are billed for a fourth round. The **next, not-yet-issued round is shown as money to collect**, priced from the airline policy via `nextEmdSuggestion`, and deliberately excluded from `required` — the airline is not holding it, so it is not arrears. Shares are cumulative-difference rounded so they sum to `required` exactly. `agentDues` now returns the EMD share first as a fact rather than by date sorting, since an undated share would otherwise rank behind a balance months away. **Dropped from the daily alert** by having no date rather than by a special case. Regression caught in passing: the send-notice button only appeared for dated obligations, so `buildAgentNotice` now takes a null date and writes "Due: before the next EMD is issued". Verified live in a rolled-back transaction: a booking with rounds 1–2 refunded and re-issued as 3–4 shows an agent holding 10 of 30 seats exactly two lines, 150,000 + 300,000 = required 450,000 to the paisa, less 200,000 received = 250,000 to collect, undated and out of the alert; with only round 1 issued, SV policy prices round 2 at 85% and the schedule shows 850,000 to collect before issuance. 496 tests, lint 0, build green |
| 2026-09-23 | Dashboard | EMDs To Issue card + Next Deadline fix | Done | New **EMDs To Issue** card: pick a date and it totals **the deposits to be issued that day** (`seats × fare × the next round's policy %`, owner's choice over booking EMD value) while the table narrows to those bookings. **That date exactly**, not on-or-before; **blank until a date is picked**; active bookings only; an amount no policy can derive is **counted separately, never zeroed** ("N need a manual amount"). Card and rows share one predicate (`matchesIssuanceDate`) so they cannot disagree — a plain column filter would have shown cancelled rows the card excluded. **Next Deadline column fixed:** it read `nextPendingDeadline` (earliest deadline among rounds already issued) and so printed "No issued round" against a booking that had never had an EMD — precisely the one with work outstanding. Rows now carry `nextIssuanceDeadline`, falling back to `pnr_tl_date` (the 1st EMD's time limit), with small text naming the round ("1st EMD", "2nd EMD") and its amount. Same fix applied to the "needs attention within 2 days" banner, which had the identical defect and was excluding first-EMD bookings from the warning meant to catch them. `nextEmdAmount` apportioned in holder views like every other money figure. Verified live in a rolled-back transaction at today=2026-09-23: five bookings, picking 20 Sep gave PKR 3,352,500 across 3 bookings with 1 manual, table matched the card exactly, cancelled booking excluded, 21 Sep returned only its own. 515 tests, lint 0, build green. **Known gap flagged in decisions.md:** a booking rolled forward under the IATA rule (rounds 1–2 re-issued as 3–4) labels the next as "5th EMD" with a manual amount, since every recorded round is counted — consistent with the detail page and bulk screen, worth revisiting for all three together |
| 2026-09-24 | Ops | Performance and security pass | Done | Measured before changing anything: 4 bookings in the database, so volume was not the cause. Three multiplying round-trip costs: **(1)** functions in Washington D.C. (Vercel default) against a Singapore database → `vercel.json` now `"regions": ["sin1"]`; **(2)** Prisma's `pgbouncer=true` mode cost ~5 round trips per query (504 ms vs 103 ms measured A/B) → `@prisma/adapter-pg` driver adapter; **(3)** one query per `include` relation and serial awaits → `relationJoins` + `Promise.all`. Booking page data **3,740 → 212 ms**, dashboard **2,067 → 117 ms** (from a laptop; production also gains the region fix). Adapter proved field-for-field identical to the old engine on every table, under UTC and Karachi time, for reads and writes; row locks still work. Auth: pages and middleware use `getClaims()` via one cached helper; write actions keep `getUser()` so revoked users are refused immediately. **Redis not added** and lookup caching dropped after measurement showed 0 ms saving. `dashboard_totals` view dropped (unused since 2026-09-09); no fields removed. `loading.tsx` skeletons on every main route, `error.tsx` that never shows raw messages. **Security:** next 15.5.23 → 15.5.26 closing two critical unauthenticated RCEs; `npm audit` 6 → 0 (two transitive pins via `overrides`); CSP (Report-Only), Permissions-Policy, `X-Powered-By` removed; AI route rate-limited; `db:security` fixed — it could not run on a database with no refunds. Production build checked: logged-out and forged-cookie requests redirect to login, cron 401. 519 tests, lint 0, build green. **Owner actions:** deploy, then confirm region `sin1`; switch Supabase to asymmetric JWT keys (rotate, do NOT revoke — steps in `operations.md`); after one clean deploy, switch CSP from Report-Only to enforced |

(Add a new row every time a step changes status. Keep old rows — this becomes the project history.)
